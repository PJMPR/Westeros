using Microsoft.VisualStudio.TestTools.UnitTesting;
using Westeros.UserProfile.Data;
using Westeros.UserProfile.Data.Model;

namespace Westeros.UserProfile.Tests
{
    [TestClass]
    public class UnitTest1
    {
        /*[TestMethod]
        public void BMI_Test()
        {
            Calculator c = new Calculator();

            User user = new User();
            user.height = 1.87;
            user.weight = 93;

            var weight = user.weight;
            var height = user.height;

            decimal uWeight = (decimal)weight;
            decimal uHeight = (decimal)height;
            decimal expected = 26.5949841287998m;
            
            var amount = Calculator.BmiCalculate(user);
            decimal actual = (decimal)amount;

            Assert.AreEqual(expected, actual);
        }

        [TestMethod]
        public void BmrCalculateTest()
        {
            UserProfile userProfile = new UserProfile();
            userProfile.Age = 25;
            userProfile.Height = 1.87;
            userProfile.Weight = 93;
            userProfile.Sex = Sex.Male;

            var userWeight = userProfile.Weight;
            var userHeight = userProfile.Height;

            decimal uWeight = (decimal)userWeight;
            decimal uHeight = (decimal)userHeight;
            decimal expected = 1979.82m;

            var amount = Calculator.BmrCalculate(userProfile);
            decimal actual = (decimal)amount;

            Assert.AreEqual(expected, actual);
        }*/
    }
}
